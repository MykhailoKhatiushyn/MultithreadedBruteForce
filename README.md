# Multithreaded Brute Force Password Resetter

A WPF application designed to demonstrate multithreaded brute-force hash matching and thread synchronization using C#.

## Version History
- **Version 1:** Implemented static salted SHA-256 hashing (`Hasher.cs`).